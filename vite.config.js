// Vite configuration file for the Prime Video project
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// Export the Vite configuration with React plugin
export default defineConfig({
  plugins: [react()]
})